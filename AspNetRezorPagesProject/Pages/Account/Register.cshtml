@page
@model AspNetRezorPagesProject.Pages.Account.RegisterModel
@{
    Layout = "_AuthLayout";
    ViewData["Title"] = "Registration";
}

<div class="auth-card">

    <h2 class="auth-title">Registration</h2>

    <form method="post" novalidate>

        <div class="form-group">
            <label asp-for="Input.Name" class="auth-label">Имя</label>
            <input asp-for="Input.Name" class="form-control" />
            <span asp-validation-for="Input.Name" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Input.Email" class="auth-label">Электронная почта</label>
            <input asp-for="Input.Email" class="form-control" />
            <span asp-validation-for="Input.Email" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Input.Password" class="auth-label">Пароль</label>
            <input asp-for="Input.Password" type="password" class="form-control" />
            <span asp-validation-for="Input.Password" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Input.ConfirmPassword" class="auth-label">Подтвердить пароль</label>
            <input asp-for="Input.ConfirmPassword" type="password" class="form-control" />
            <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
        </div>

        <button type="submit" class="btn-primary">Зарегистрироваться</button>

        <div class="auth-footer">
            <span>Есть аккаунт?</span>
            <a asp-page="/Account/Login">Войти</a>
        </div>

    </form>

</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
